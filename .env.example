GEMINI_API_KEY=
RESEND_API_KEY=
# Optional: verified Resend sender, e.g. "Morning Brief <brief@yourdomain.com>"
# Falls back to Resend's sandbox sender if unset (limited recipient support).
RESEND_FROM=
