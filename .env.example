GEMINI_API_KEY=
# Gmail address to send the brief from, and a 16-character App Password for
# it (Google Account → Security → 2-Step Verification → App passwords).
# A regular Gmail password will NOT work here.
GMAIL_ADDRESS=
GMAIL_APP_PASSWORD=
